# Dependencies
node_modules/
package-lock.json
__pycache__/
*.pyc

# Runtime
orchestrator/orchestrator.log

# Per-project mailbox and workspace (created at runtime by start.sh)
shared/*/mailbox/to_dev/*.json
shared/*/mailbox/to_qa/*.json
shared/*/workspace/*

# Legacy single-project patterns (kept for backwards compat)
shared/mailbox/to_dev/*.json
shared/mailbox/to_qa/*.json
shared/workspace/*

# Keep gitkeep files
!**/.gitkeep

# Environment
.env
.env.*

# OS
.DS_Store
